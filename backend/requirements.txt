# Web framework
fastapi>=0.110.0
uvicorn>=0.28.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
python-dotenv>=1.0.0

# Auth & validation
pydantic[email]>=2.6.0
pydantic-settings>=2.1.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 can't read bcrypt>=4.1 ("module 'bcrypt' has no attribute
# '__about__'") and bcrypt 5 errors on its backend probe — pin to the last
# compatible release so password hashing works.
bcrypt==4.0.1
# argon2 is the PRIMARY password hash (app/core/security.py uses argon2id);
# passlib needs the argon2_cffi backend installed or hashing/verifying fails.
argon2-cffi>=23.1.0

# Ask-AI LLM router (app/services/ask_ai): multi-provider completion +
# token counting. `groq` is litellm's SDK dependency for the Groq provider.
# Capped below 1.92: that release ships no wheel for Python 3.13, so pip falls
# back to building it from source and fails without a Rust toolchain. Raise the
# cap once 1.92+ publishes 3.13 wheels.
litellm>=1.40.0,<1.92.0
groq>=0.9.0
pyyaml>=6.0

# Testing / HTTP client
httpx>=0.27.0
