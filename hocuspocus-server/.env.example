# Local-dev port. Hosts like Render inject $PORT and that takes priority — see server.js.
COLLAB_PORT=1234
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/docplatform
JWT_SECRET=your-secret-key-same-as-fastapi
